{
  "name": "oc-lint",
  "version": "0.0.1",
  "description": "Linter for OpenClonk Script",
  "main": "main.js",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.ts",
    "test-tokenizer": "mocha -r ts-node/register tests/tokenizer/*.test.ts",
    "test-parser": "mocha -r ts-node/register tests/parser/*.test.ts",
    "test-exp": "mocha -r ts-node/register tests/precedence-parser/*.test.ts",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "start": "node ./out/main.js",
    "dev": "npm run compile & npm run start"
  },
  "keywords": [
    "linter",
    "openclonk"
  ],
  "author": "Fridjof Milke",
  "license": "MIT",
  "devDependencies": {
    "@types/chai": "^4.2.8",
    "@types/mocha": "^7.0.1",
    "@types/node": "^13.7.0",
    "@types/yaml": "^1.2.0",
    "chai": "^4.2.0",
    "mocha": "^7.0.1",
    "ts-node": "^8.6.2",
    "typescript": "^3.7.5",
    "yaml": "^1.7.2"
  }
}
